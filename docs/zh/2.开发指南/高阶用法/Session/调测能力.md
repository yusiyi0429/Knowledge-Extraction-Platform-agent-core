Session提供的调测模块，方便用户进行调测。该模块在Workflow的执行过程中自动化的采集事件信息，并以**流式消息**的形式实时的发送给用户，将系统内部状态透明化地呈现给用户。也支持用户注入自定义的调测信息。openJiuwen提供了以下两种调测能力：

* **基础调测能力**： Session的调测在Workflow模式下，记录了整个工作流的执行开始和最终完成的状态。并且对Workflow整个执行过程中进行更加细致的事件采集，包括：
  
  * **组件执行**：追踪每个组件的开始、结束与执行状态。
  * **复杂逻辑增强**：针对**循环、条件分支**等复杂结构，进行增强式采集。
  * **执行异常**：Workflow在执行过程中抛出的错误会被自动捕获。
* **自定义调测能力**：Session还提供了标准调测信息收集接口，允许开发者注入并收集**自定义调测信息**。

# 基础调测能力

调测字段参考API接口文档[openjiuwen.core.session.stream.TraceSchema](../../API文档/openjiuwen.core/session/stream/stream.md#class-openjiuwencoresessionstreamtraceschema)。

以下将从三种典型调测场景出发，介绍基础工作流调测、循环组件调测和嵌套子工作流调测的关键字段与输出示例。

## 基础工作流场景

以搭建一个基础工作流为例介绍基础调测信息。

自定义组件`CustomComponent`，将输入直接作为输出返回，便于后续调测信息的说明：

```python
from openjiuwen.core.workflow import WorkflowComponent
from openjiuwen.core.context_engine import ModelContext
from openjiuwen.core.workflow import Input, Output
from openjiuwen.core.workflow.components import Session

# 定义一个自定义组件
class CustomComponent(WorkflowComponent):
    def __init__(self):
        super().__init__()

    async def invoke(self, inputs: Input, session: Session, context: ModelContext) -> Output:
        return inputs
```

搭建基础工作流，流程定义为start -> a -> end，工作流最终输出的内容等于调用时输入的内容：

```python
from openjiuwen.core.workflow import Workflow
from openjiuwen.core.workflow import End
from openjiuwen.core.workflow import Start
"""
搭建基础工作流
start -> a -> end
"""
workflow = Workflow()
# 设置start组件
workflow.set_start_comp("start", Start(), inputs_schema={"num": "${num}"})
# 设置自定义组件a
workflow.add_workflow_comp("a", CustomComponent(), inputs_schema={"a_num": "${start.num}"})
# 设置结束组件end
workflow.set_end_comp("end", End(conf={"responseTemplate": "hello:{{result}}"}),
                      inputs_schema={"result": "${a.a_num}"})
# 连接组件： start -> a -> end
workflow.add_connection("start", "a")
workflow.add_connection("a", "end")
```

通过`flow.stream`方法执行工作流，并指定`stream_modes=[BaseStreamMode.TRACE]`，获取基础工作流的流输出调测信息：

```python
import asyncio
from openjiuwen.core.workflow import create_workflow_session
from openjiuwen.core.session.stream import BaseStreamMode

async def run_workflow():
    async for chunk in workflow.stream({"num": 1}, create_workflow_session(), stream_modes=[BaseStreamMode.TRACE]):
        print(f"stream chunk: {chunk}")


if __name__ == '__main__':
    asyncio.run(run_workflow())
```

截取组件`a`的调测信息如下，获取到两帧信息，包含一帧开始事件和一帧结束事件。其中，`traceId`标识此次调测事件。
- 开始事件包含特殊字段`startTime`，并且`status`为`“start”`。
- 结束事件包含特殊字段`endTime`，`status`对应`“finish”`。

```python
...
# 组件a的调测开始事件帧
TraceSchema(type = 'tracer_workflow', payload = {
    'traceId': '1297a59f-3d09-4a3e-8b08-79b9db40aea9',
    'startTime': datetime.datetime(2026, 2, 4, 10, 47, 41, 944183),
    'inputs': {'a_num': 1},
    'invokeId': 'a',
    'parentInvokeId': 'start',
    'status': 'start',
    'onInvokeData': [],
    'executionId': '1297a59f-3d09-4a3e-8b08-79b9db40aea9',
    'workflowId': '61bc52a6dc17426e991c3665be98774d',
    'componentId': 'a',
    'componentName': 'a',
    'componentType': 'CustomComponent',
    'parentNodeId': ''
})
# 组件a的调测结束事件帧
TraceSchema(type = 'tracer_workflow', payload = {
    'traceId': '1297a59f-3d09-4a3e-8b08-79b9db40aea9',
    'startTime': datetime.datetime(2026, 2, 4, 10, 47, 41, 944183),
    'endTime': datetime.datetime(2026, 2, 4, 10, 47, 41, 945187),
    'inputs': {'a_num': 1},
    'outputs': {'a_num': 1},
    'invokeId': 'a',
    'parentInvokeId': 'start',
    'status': 'finish',
    'onInvokeData': [],
    'executionId': '1297a59f-3d09-4a3e-8b08-79b9db40aea9',
    'workflowId': '61bc52a6dc17426e991c3665be98774d',
    'componentId': 'a',
    'componentName': 'a',
    'componentType': 'CustomComponent',
    'parentNodeId': '',
    'streamOutputs': []
})
...
```

## 循环工作流场景

以搭建一个循环工作流为例介绍循环组件调测信息。
同样使用自定义组件`CustomComponent`搭建一个包含循环组件、开始组件、结束组件的工作流，循环组件使用数组循环模式，其中循环体内有三个串联的自定义组件`a`，`b`，`c`，分别输出每次循环迭代的数组元素：

```python
from openjiuwen.core.workflow import WorkflowComponent
from openjiuwen.core.workflow import LoopGroup, LoopComponent
from openjiuwen.core.context_engine import ModelContext
from openjiuwen.core.workflow import Input, Output
from openjiuwen.core.workflow.components import Session
from openjiuwen.core.workflow import Workflow
from openjiuwen.core.workflow import End
from openjiuwen.core.workflow import Start

# 自定义组件返回组件输入中"value"字段的值，赋值给output变量
class CustomComponent(WorkflowComponent):
    async def invoke(self, inputs: Input, session: Session, context: ModelContext) -> Output:
        return {"output": inputs["value"]}

# 创建LoopGroup
loop_group = LoopGroup()

# 为LoopGroup添加3个工作流组件(以数组循环为例)
loop_group.add_workflow_comp("a", CustomComponent(), inputs_schema={"value":"${loop.item}"})
loop_group.add_workflow_comp("b", CustomComponent(), inputs_schema={"value":"${loop.item}"})
loop_group.add_workflow_comp("c", CustomComponent(), inputs_schema={"value":"${loop.item}"})

# 指定组件"a"为循环开始，组件"c"为循环结束
loop_group.start_nodes(["a"])
loop_group.end_nodes(["c"])

# LoopGroup中的连接3个组件，a->b->c
loop_group.add_connection("a", "b")
loop_group.add_connection("b", "c")

# 创建循环组件，使用数组循环，输入数组为循环组件输入schema中的"input"字段值
loop_component = LoopComponent(
    loop_group,
    {"output": {"a": "${a.output}", "b": "${b.output}", "c": "${c.output}"}}
)

workflow = Workflow()

# 添加开始结束组件，结束组件的输入引用loop组件的输出
workflow.set_start_comp("start", Start(), inputs_schema={"query": "${array}"})
workflow.set_end_comp("end", End(), inputs_schema={"user_var": "${loop.output}"})

# 添加循环组件，输入的数组为开始组件的query输出
workflow.add_workflow_comp("loop", loop_component, inputs_schema={"loop_type": "array", "loop_array": {"item": "${start.query}"}})

# 串行连接组件：start->loop->end
workflow.add_connection("start", "loop")
workflow.add_connection("loop", "end")
```

通过`flow.stream`方法执行工作流，并指定`stream_modes=[BaseStreamMode.TRACE]`，获取循环工作流的流输出调测信息：

```python
import asyncio
from openjiuwen.core.workflow import create_workflow_session
from openjiuwen.core.session.stream import BaseStreamMode

async def run_workflow():
    async for chunk in workflow.stream({"array": [1, 2, 3]}, session=create_workflow_session(), stream_modes=[BaseStreamMode.TRACE]):
        print(f"stream chunk: {chunk}")

if __name__ == "__main__":
    asyncio.run(run_workflow())
```

截取位于循环体中的组件`a`的调测信息如下，获取到两帧信息，包含一帧开始事件和一帧结束事件。其中，`loopIndex`表示该组件位于循环组件`loop`中，值为3，说明当前处于第3次循环执行状态。与此同时，循环体内组件`a`的`invokeId`为`loop.a`，而其`parentInvokeId`为`loop.c`，表示上一次执行的组件为上一轮循环中循环体内的组件`c`。

```python
...
# 循环组件中组件a的第3次运行，调测开始帧
TraceSchema(type = 'tracer_workflow', payload = {
    'traceId': '51b131ba-f938-4b8b-a4c9-f0fc9b034c2e',
    'startTime': datetime.datetime(2026, 2, 4, 11, 51, 13, 173222),
    'inputs': {'value': 3},
    'invokeId': 'loop.a',
    'status': 'start',
    'onInvokeData': [],
    'executionId': '51b131ba-f938-4b8b-a4c9-f0fc9b034c2e',
    'workflowId': 'bbf51a26088045f09b9ce0cf7125fa45',
    'componentId': 'a',
    'componentName': 'a',
    'componentType': 'CustomComponent',
    'loopNodeId': 'loop',
    'loopIndex': 2,
    'parentNodeId': 'loop'
})
# 循环组件中组件a的第3次运行，调测结束帧
TraceSchema(type = 'tracer_workflow', payload = {
    'traceId': '51b131ba-f938-4b8b-a4c9-f0fc9b034c2e',
    'startTime': datetime.datetime(2026, 2, 4, 11, 51, 13, 173222),
    'endTime': datetime.datetime(2026, 2, 4, 11, 51, 13, 174404),
    'inputs': {'value': 3},
    'outputs': {'output': 3},
    'invokeId': 'loop.a',
    'status': 'finish',
    'onInvokeData': [],
    'executionId': '51b131ba-f938-4b8b-a4c9-f0fc9b034c2e',
    'workflowId': 'bbf51a26088045f09b9ce0cf7125fa45',
    'componentId': 'a',
    'componentName': 'a',
    'componentType': 'CustomComponent',
    'loopNodeId': 'loop',
    'loopIndex': 2,
    'parentNodeId': 'loop',
    'streamOutputs': []})
...
```

## 嵌套子工作流场景

以搭建一个嵌套工作流为例介绍嵌套子工作流调测信息。

首先搭建一个子工作流，包含开始组件、结束组件和之前自定义的`CustomComponent`，子工作流的输出等于调用工作流时的输入：

```python
from openjiuwen.core.workflow import WorkflowComponent
from openjiuwen.core.context_engine import ModelContext
from openjiuwen.core.workflow import Input, Output
from openjiuwen.core.workflow.components import Session
from openjiuwen.core.workflow import Workflow
from openjiuwen.core.workflow import End
from openjiuwen.core.workflow import Start

# 自定义组件返回组件输入中"value"字段的值，赋值给output变量
class CustomComponent(WorkflowComponent):
    async def invoke(self, inputs: Input, session: Session, context: ModelContext) -> Output:
        return {"a_num": inputs["a_num"]}

# 子workflow: sub_start -> sub_a -> sub_end
sub_workflow = Workflow()
# 子workflow的开始组件为sub_start
sub_workflow.set_start_comp("sub_start", Start(),
                            inputs_schema={
                                "num": "${a_num}"})
# 子workflow的组件sub_a
sub_workflow.add_workflow_comp("sub_a", CustomComponent(),
                               inputs_schema={
                                   "a_num": "${sub_start.num}"})
# 子workflow的结束组件sub_end
sub_workflow.set_end_comp("sub_end",
                          End(conf={"responseTemplate": "hello:{{result}}"}),
                          inputs_schema={
                              "result": "${sub_a.a_num}"
                          })
# 连接子workflow各组件：sub_start -> sub_a -> sub_end
sub_workflow.add_connection("sub_start", "sub_a")
sub_workflow.add_connection("sub_a", "sub_end")
```

构建嵌套工作流，包含开始组件、结束组件和使用子工作流作为主工作流的组件"a"，主工作流的输出也等于主工作流调用时的输入：

```python
from openjiuwen.core.workflow import SubWorkflowComponent

"""
workflow: start -> a(子workflow) -> end
子workflow: sub_start -> sub_a -> sub_end
"""
# workflow: start->a(子workflow)->end
main_workflow = Workflow()
# 设置workflow的开始组件为start
main_workflow.set_start_comp("start", Start(),
                             inputs_schema={
                                 "num": "${num}"})
# 定义workflow组件a
main_workflow.add_workflow_comp("a", SubWorkflowComponent(sub_workflow),
                                inputs_schema={
                                    "a_num": "${start.num}"})
# 设置workflow的结束组件为end
main_workflow.set_end_comp("end",
                           End(conf={"responseTemplate": "hello:{{result}}"}),
                           inputs_schema={
                               "result": "${a.a_num}"
                           })
# 连接workflow各组件：start -> a -> end
main_workflow.add_connection("start", "a")
main_workflow.add_connection("a", "end")
```

通过`flow.stream`方法执行工作流，并指定`stream_modes=[BaseStreamMode.TRACE]`，获取嵌套工作流的流输出调测信息：

```python
import asyncio
from openjiuwen.core.workflow import create_workflow_session
from openjiuwen.core.session.stream import BaseStreamMode


async def run_workflow():
    async for chunk in main_workflow.stream({"num": 1}, create_workflow_session(),
                                            stream_modes=[BaseStreamMode.TRACE]):
        print(f"stream chunk: {chunk}")


if __name__ == "__main__":
    asyncio.run(run_workflow())
```

截取位于子工作流的`sub_a`组件调测信息如下，获取到两帧信息，包含一帧开始事件和一帧结束事件。其中，该组件的`parentNodeId`字段记录其所属的子工作流的`invokeId`，即`"a"`。该字段主要用于嵌套子工作流的场景，以便明确组件的层级关系和归属结构。同时，`sub_a`组件位于子工作流组件`a`中，因此其`invokeId`为`"a.sub_a"`，用于标识该组件在整个工作流中的调用。

```python
...
# 子workflow中的sub_a组件的调测开始帧
TraceSchema(type = 'tracer_workflow', payload = {
    'traceId': 'c652fc32-6ac8-4d75-942f-aed4dd615d55',
    'startTime': datetime.datetime(2026, 2, 4, 12, 0, 9, 232875),
    'inputs': {'a_num': 1},
    'invokeId': 'a.sub_a',
    'parentInvokeId': 'a.sub_start',
    'status': 'start', 'onInvokeData': [],
    'executionId': 'c652fc32-6ac8-4d75-942f-aed4dd615d55',
    'workflowId': 'd63aa59a0243451cbde3e4dd68129e59',
    'componentId': 'sub_a',
    'componentName': 'sub_a',
    'componentType': 'CustomComponent',
    'parentNodeId': 'a'
}), 
# 子workflow中的sub_a组件的调测结束帧
TraceSchema(type = 'tracer_workflow', payload = {
    'traceId': 'c652fc32-6ac8-4d75-942f-aed4dd615d55',
    'startTime': datetime.datetime(2026, 2, 4, 12, 0, 9, 232875),
    'endTime': datetime.datetime(2026, 2, 4, 12, 0, 9, 232875),
    'inputs': {'a_num': 1},
    'outputs': {'a_num': 1},
    'invokeId': 'a.sub_a',
    'parentInvokeId': 'a.sub_start',
    'status': 'finish',
    'onInvokeData': [],
    'executionId': 'c652fc32-6ac8-4d75-942f-aed4dd615d55',
    'workflowId': 'd63aa59a0243451cbde3e4dd68129e59',
    'componentId': 'sub_a',
    'componentName': 'sub_a',
    'componentType': 'CustomComponent',
    'parentNodeId': 'a',
    'streamOutputs': []
})
...
```

# 自定义调测能力

为了方便开发者调试自定义的组件，Session 专门提供了接口，允许开发者向组件中注入自定义的调试信息。

创建自定义组件`AddComponent`，用来做加法运算，并调用Session的`trace`和`trace_error`能力来记录自定义的调测信息。

```python
import time

from openjiuwen.core.common.exception.exception import JiuWenBaseException
from openjiuwen.core.workflow import WorkflowComponent
from openjiuwen.core.context_engine import ModelContext
from openjiuwen.core.workflow import Input, Output
from openjiuwen.core.workflow.components import Session


class AddComponent(WorkflowComponent):
    def __init__(self):
        super().__init__()


    async def invoke(self, inputs: Input, session: Session, context: ModelContext) -> Output:
        a = inputs.get('a')
        b = inputs.get('b')
        if not a:
            error = JiuWenBaseException(-1, 'a is not exit')
            await session.trace_error(error)
            raise error
        if not b:
            error = JiuWenBaseException(-1, 'b is not exit')
            await session.trace_error(error)
            raise error
        start_time = time.time()
        result = a + b
        cost = time.time() - start_time
        await session.trace({"a": a, "b": b, "cost": cost})
        return {"output": result}

```

构建简单工作流，将自定义的`AddComponent`添加到工作流中。

```python
from openjiuwen.core.workflow import Workflow
from openjiuwen.core.workflow import End
from openjiuwen.core.workflow import Start

flow = Workflow()
flow.set_start_comp("start", Start())
flow.add_workflow_comp("add", AddComponent(), inputs_schema={"a": "${inputs.a}", "b": "${inputs.b}"})
flow.set_end_comp("end", End(), inputs_schema={"result": "${add.output}"})
flow.add_connection("start", "add")
flow.add_connection("add", "end")
```

调用工作流的`stream`接口，打印trace信息。

```python
import asyncio
from openjiuwen.core.workflow import create_workflow_session
from openjiuwen.core.session.stream import BaseStreamMode

async def run_workflow():
    async for chunk in flow.stream(inputs={"inputs": {"a": 1, "b": 2}}, session=create_workflow_session(),
                                   stream_modes=[BaseStreamMode.TRACE]):
        print(chunk)
        
if __name__ == "__main__":
    asyncio.run(run_workflow())
```

输出结果如下，其中自定义trace信息作为第5帧流输出。

```python
type='tracer_workflow' payload={'traceId': '068cf0c9-3155-45bf-bfe9-6bbdac30377c', 'startTime': datetime.datetime(2026, 2, 4, 14, 6, 26, 222945), 'inputs': {'inputs': {'a': 1, 'b': 2}}, 'invokeId': 'd4cb21924c9f43c5882b6bfbc66a5bc5', 'status': 'start', 'onInvokeData': [], 'executionId': '068cf0c9-3155-45bf-bfe9-6bbdac30377c', 'workflowId': 'd4cb21924c9f43c5882b6bfbc66a5bc5', 'workflowVersion': '', 'workflowName': '', 'parentNodeId': '', 'streamOutputs': []}
type='tracer_workflow' payload={'traceId': '068cf0c9-3155-45bf-bfe9-6bbdac30377c', 'startTime': datetime.datetime(2026, 2, 4, 14, 6, 26, 253497), 'invokeId': 'start', 'parentInvokeId': 'd4cb21924c9f43c5882b6bfbc66a5bc5', 'status': 'start', 'onInvokeData': [], 'executionId': '068cf0c9-3155-45bf-bfe9-6bbdac30377c', 'workflowId': 'd4cb21924c9f43c5882b6bfbc66a5bc5', 'componentId': 'start', 'componentName': 'start', 'componentType': 'Start', 'parentNodeId': ''}
type='tracer_workflow' payload={'traceId': '068cf0c9-3155-45bf-bfe9-6bbdac30377c', 'startTime': datetime.datetime(2026, 2, 4, 14, 6, 26, 253497), 'endTime': datetime.datetime(2026, 2, 4, 14, 6, 26, 253497), 'invokeId': 'start', 'parentInvokeId': 'd4cb21924c9f43c5882b6bfbc66a5bc5', 'status': 'finish', 'onInvokeData': [], 'executionId': '068cf0c9-3155-45bf-bfe9-6bbdac30377c', 'workflowId': 'd4cb21924c9f43c5882b6bfbc66a5bc5', 'componentId': 'start', 'componentName': 'start', 'componentType': 'Start', 'parentNodeId': '', 'streamOutputs': []}
type='tracer_workflow' payload={'traceId': '068cf0c9-3155-45bf-bfe9-6bbdac30377c', 'startTime': datetime.datetime(2026, 2, 4, 14, 6, 26, 254700), 'inputs': {'a': 1, 'b': 2}, 'invokeId': 'add', 'parentInvokeId': 'start', 'status': 'start', 'onInvokeData': [], 'executionId': '068cf0c9-3155-45bf-bfe9-6bbdac30377c', 'workflowId': 'd4cb21924c9f43c5882b6bfbc66a5bc5', 'componentId': 'add', 'componentName': 'add', 'componentType': 'AddComponent', 'parentNodeId': ''}
type='tracer_workflow' payload={'traceId': '068cf0c9-3155-45bf-bfe9-6bbdac30377c', 'startTime': datetime.datetime(2026, 2, 4, 14, 6, 26, 254700), 'inputs': {'a': 1, 'b': 2}, 'invokeId': 'add', 'parentInvokeId': 'start', 'status': 'running', 'onInvokeData': [{'a': 1, 'b': 2, 'cost': 0.0}], 'executionId': '068cf0c9-3155-45bf-bfe9-6bbdac30377c', 'workflowId': 'd4cb21924c9f43c5882b6bfbc66a5bc5', 'componentId': 'add', 'componentName': 'add', 'componentType': 'AddComponent', 'parentNodeId': '', 'streamOutputs': []}
type='tracer_workflow' payload={'traceId': '068cf0c9-3155-45bf-bfe9-6bbdac30377c', 'startTime': datetime.datetime(2026, 2, 4, 14, 6, 26, 254700), 'endTime': datetime.datetime(2026, 2, 4, 14, 6, 26, 255270), 'inputs': {'a': 1, 'b': 2}, 'outputs': {'output': 3}, 'invokeId': 'add', 'parentInvokeId': 'start', 'status': 'finish', 'onInvokeData': [{'a': 1, 'b': 2, 'cost': 0.0}], 'executionId': '068cf0c9-3155-45bf-bfe9-6bbdac30377c', 'workflowId': 'd4cb21924c9f43c5882b6bfbc66a5bc5', 'componentId': 'add', 'componentName': 'add', 'componentType': 'AddComponent', 'parentNodeId': '', 'streamOutputs': []}
type='tracer_workflow' payload={'traceId': '068cf0c9-3155-45bf-bfe9-6bbdac30377c', 'startTime': datetime.datetime(2026, 2, 4, 14, 6, 26, 256310), 'inputs': {'result': 3}, 'invokeId': 'end', 'parentInvokeId': 'add', 'status': 'start', 'onInvokeData': [], 'executionId': '068cf0c9-3155-45bf-bfe9-6bbdac30377c', 'workflowId': 'd4cb21924c9f43c5882b6bfbc66a5bc5', 'componentId': 'end', 'componentName': 'end', 'componentType': 'End', 'parentNodeId': ''}
type='tracer_workflow' payload={'traceId': '068cf0c9-3155-45bf-bfe9-6bbdac30377c', 'startTime': datetime.datetime(2026, 2, 4, 14, 6, 26, 256310), 'endTime': datetime.datetime(2026, 2, 4, 14, 6, 26, 256310), 'inputs': {'result': 3}, 'outputs': {'output': {'result': 3}}, 'invokeId': 'end', 'parentInvokeId': 'add', 'status': 'finish', 'onInvokeData': [], 'executionId': '068cf0c9-3155-45bf-bfe9-6bbdac30377c', 'workflowId': 'd4cb21924c9f43c5882b6bfbc66a5bc5', 'componentId': 'end', 'componentName': 'end', 'componentType': 'End', 'parentNodeId': '', 'streamOutputs': []}
type='tracer_workflow' payload={'traceId': '068cf0c9-3155-45bf-bfe9-6bbdac30377c', 'startTime': datetime.datetime(2026, 2, 4, 14, 6, 26, 222945), 'endTime': datetime.datetime(2026, 2, 4, 14, 6, 26, 257465), 'inputs': {'inputs': {'a': 1, 'b': 2}}, 'outputs': {'output': {'result': 3}}, 'invokeId': 'd4cb21924c9f43c5882b6bfbc66a5bc5', 'status': 'finish', 'onInvokeData': [], 'executionId': '068cf0c9-3155-45bf-bfe9-6bbdac30377c', 'workflowId': 'd4cb21924c9f43c5882b6bfbc66a5bc5', 'workflowVersion': '', 'workflowName': '', 'parentNodeId': '', 'streamOutputs': []}
type='workflow_final' index=0 payload={'output': {'result': 3}}

```

若重新调用工作流的`stream`接口，触发到`AddComponent`的错误场景。

```python
import asyncio
from openjiuwen.core.workflow import create_workflow_session
from openjiuwen.core.session.stream import BaseStreamMode

async def run_workflow():
    async for chunk in flow.stream(inputs={"inputs": {"b": 2}}, session=create_workflow_session(),
                                   stream_modes=[BaseStreamMode.TRACE]):
        print(chunk)
        
if __name__ == "__main__":
    asyncio.run(run_workflow())
```

输出结果如下，其中倒数第二帧为自定义的错误调测信息。

```python
type='tracer_workflow' payload={'traceId': 'bbe6e6e6-4324-4a1b-8163-9a5386a1d2e8', 'startTime': datetime.datetime(2026, 2, 4, 14, 12, 9, 165528), 'inputs': {'inputs': {'b': 2}}, 'invokeId': '111bc5684a064fccb382403f6fcab317', 'status': 'start', 'onInvokeData': [], 'executionId': 'bbe6e6e6-4324-4a1b-8163-9a5386a1d2e8', 'workflowId': '111bc5684a064fccb382403f6fcab317', 'workflowVersion': '', 'workflowName': '', 'parentNodeId': '', 'streamOutputs': []}
type='tracer_workflow' payload={'traceId': 'bbe6e6e6-4324-4a1b-8163-9a5386a1d2e8', 'startTime': datetime.datetime(2026, 2, 4, 14, 12, 9, 191482), 'invokeId': 'start', 'parentInvokeId': '111bc5684a064fccb382403f6fcab317', 'status': 'start', 'onInvokeData': [], 'executionId': 'bbe6e6e6-4324-4a1b-8163-9a5386a1d2e8', 'workflowId': '111bc5684a064fccb382403f6fcab317', 'componentId': 'start', 'componentName': 'start', 'componentType': 'Start', 'parentNodeId': ''}
type='tracer_workflow' payload={'traceId': 'bbe6e6e6-4324-4a1b-8163-9a5386a1d2e8', 'startTime': datetime.datetime(2026, 2, 4, 14, 12, 9, 191482), 'endTime': datetime.datetime(2026, 2, 4, 14, 12, 9, 191482), 'invokeId': 'start', 'parentInvokeId': '111bc5684a064fccb382403f6fcab317', 'status': 'finish', 'onInvokeData': [], 'executionId': 'bbe6e6e6-4324-4a1b-8163-9a5386a1d2e8', 'workflowId': '111bc5684a064fccb382403f6fcab317', 'componentId': 'start', 'componentName': 'start', 'componentType': 'Start', 'parentNodeId': '', 'streamOutputs': []}
type='tracer_workflow' payload={'traceId': 'bbe6e6e6-4324-4a1b-8163-9a5386a1d2e8', 'startTime': datetime.datetime(2026, 2, 4, 14, 12, 9, 191482), 'inputs': {'a': None, 'b': 2}, 'invokeId': 'add', 'parentInvokeId': 'start', 'status': 'start', 'onInvokeData': [], 'executionId': 'bbe6e6e6-4324-4a1b-8163-9a5386a1d2e8', 'workflowId': '111bc5684a064fccb382403f6fcab317', 'componentId': 'add', 'componentName': 'add', 'componentType': 'AddComponent', 'parentNodeId': ''}
type='tracer_workflow' payload={'traceId': 'bbe6e6e6-4324-4a1b-8163-9a5386a1d2e8', 'startTime': datetime.datetime(2026, 2, 4, 14, 12, 9, 191482), 'endTime': datetime.datetime(2026, 2, 4, 14, 12, 9, 193005), 'inputs': {'a': None, 'b': 2}, 'error': {'error_code': -1, 'message': 'a is not exit'}, 'invokeId': 'add', 'parentInvokeId': 'start', 'status': 'error', 'onInvokeData': [], 'executionId': 'bbe6e6e6-4324-4a1b-8163-9a5386a1d2e8', 'workflowId': '111bc5684a064fccb382403f6fcab317', 'componentId': 'add', 'componentName': 'add', 'componentType': 'AddComponent', 'parentNodeId': '', 'streamOutputs': []}
type='tracer_workflow' payload={'traceId': 'bbe6e6e6-4324-4a1b-8163-9a5386a1d2e8', 'startTime': datetime.datetime(2026, 2, 4, 14, 12, 9, 191482), 'endTime': datetime.datetime(2026, 2, 4, 14, 12, 9, 193005), 'inputs': {'a': None, 'b': 2}, 'error': {'error_code': 100005, 'message': 'workflow component runtime error, reason: node_id: add, ability: invoke, error: [-1] a is not exit'}, 'invokeId': 'add', 'parentInvokeId': 'start', 'status': 'error', 'onInvokeData': [], 'executionId': 'bbe6e6e6-4324-4a1b-8163-9a5386a1d2e8', 'workflowId': '111bc5684a064fccb382403f6fcab317', 'componentId': 'add', 'componentName': 'add', 'componentType': 'AddComponent', 'parentNodeId': '', 'streamOutputs': []}
type='tracer_workflow' payload={'traceId': 'bbe6e6e6-4324-4a1b-8163-9a5386a1d2e8', 'startTime': datetime.datetime(2026, 2, 4, 14, 12, 9, 165528), 'endTime': datetime.datetime(2026, 2, 4, 14, 12, 9, 204786), 'inputs': {'inputs': {'b': 2}}, 'invokeId': '111bc5684a064fccb382403f6fcab317', 'status': 'finish', 'onInvokeData': [], 'executionId': 'bbe6e6e6-4324-4a1b-8163-9a5386a1d2e8', 'workflowId': '111bc5684a064fccb382403f6fcab317', 'workflowVersion': '', 'workflowName': '', 'parentNodeId': '', 'streamOutputs': []}
```
