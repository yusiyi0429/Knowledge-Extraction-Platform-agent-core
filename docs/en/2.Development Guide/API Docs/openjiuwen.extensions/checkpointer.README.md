# checkpointer

`openjiuwen.extensions.checkpointer` provides optional checkpoint extension implementations, supporting persistent storage based on Redis.

**Modules**:

| MODULE | DESCRIPTION |
|---|---|
| [redis](./checkpointer/checkpointer.md) | Redis-based checkpoint implementation supporting both standalone Redis and Redis Cluster modes. |
